#include <stdio.h>
int main()
{
    int n = printf("Hello World"); // printf() returns the number of characters printed
    printf("%d \n", n);            // This will print the number of characters printed by printf()
    return 0;
}